# Socratink evidence sources

Private. Search; do not reprint. Product needle only.

## Product (canonical)

`/Users/jondev/dev/active/socratink/product/socratink`

Needle paths: `src/ui/`, `src/agents/`, `src/app.ts`, `src/server/`,
`src/braintrust.ts`. Verify: `pnpm check`, `pnpm smoke`. Live Braintrust
only when that was the asked outcome.

Doctrine: `AGENTS.md`, `ZEN.md`. Learning claims: sibling `socratink-brain`
via `.agents/skills/socratink-brain` — confirm `CURRENT STATE.md` hash.

Not product: `prototypes/`, archive, `praxist_task/`, `chat-histories/`.

## Snapshots

`/Users/jondev/dev/active/socratink/chat-histories/` — Socratink-related
copies. Refresh only with `tooling/export_socratink_chat_histories.py` when
the user asks.

## Live stores (if snapshot is stale)

| Harness | Typical roots |
|---|---|
| Cursor | `~/.cursor/projects/*socratink*/agent-transcripts/` |
| Codex | `~/.codex/sessions/` |
| Claude | `~/.claude/projects/` |

Skip binaries, WAL/SHM, credentials. Prefer `.jsonl`.

## Skills that correlate with needle wins

- `socratink-brain` — before product/learning work
- `karpathy-guidelines` — surgical diffs
- `flue-wiki` — Chat/agent/harness API
- `code-review` / `better-ui` — when UI is the needle
- `unlazy` — gates when the loop keeps stalling at 80%

Praxist `praxist-control` / diagnostic: operator only, not a product win.
