# Optional root AGENTS.md addition (FM-DOCPRUNE-01) — NOT applied

Add under Working method or Boundaries / Always:

> Research under `research/` is load-on-demand. Prefer `research/*/AGENTS.md` pointers. Never preload `research/chat-signal/agenteng-runs`, smell/pilot JSON packs, or Brain `90 Archive` / `10 Sources` unless the task names provenance or archaeology.

Optional Commands thin: replace the long `pnpm test:*` list with a pointer to the narrowest package script, then `pnpm check` / `pnpm smoke` at handoff.
